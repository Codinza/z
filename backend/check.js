import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({where: {email: 'ayman01aay@gmail.com'}}); console.log('USER:', user); if(user) { const bcrypt = await import('bcryptjs'); const isMatch = await bcrypt.default.compare('admin123', user.password); console.log('Password match:', isMatch); } } main().finally(() => prisma.$disconnect());
