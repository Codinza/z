const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const email = 'ayman01aay@gmail.com'; const password = 'admin123'; let user = await prisma.user.findUnique({ where: { email } }); console.log('User found:', user !== null); if(user) { const valid = await bcrypt.compare(password, user.password); console.log('Password valid:', valid); } } main().finally(() => prisma.$disconnect());
